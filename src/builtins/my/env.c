/*
** EPITECH PROJECT, 2022
** 42sh
** File description:
** env command
*/

#include "42sh.h"

int my_env(char **args, shell_t *shell)
{
    if (!args || !shell)
        return FAILURE;
    return SUCCESS;
}
